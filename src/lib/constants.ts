/**
 * 全局常量
 */

/**
 * API key 掩码：服务端读取设置/供应商配置时，会用该字符串替代真实 api_key 返回给前端，
 * 防止密钥被客户端日志/前端意外回显。前端回写设置时，如果 api_key 仍为该掩码，
 * 服务端会保留数据库中原有的真实 key，不做覆盖。
 *
 * 注意：任何用于 mask 比较的代码都必须 import 这个常量，禁止散落硬编码字面量。
 */
export const API_KEY_MASK = '********';
